# 판매성과 대시보드 배포 가이드

## 구조 먼저 이해하기

이 대시보드는 **두 곳**에 나뉘어 배포됩니다.

| 파일 | 역할 | 배포 위치 |
|------|------|-----------|
| `index.html` | 화면 (UI) | **GitHub Pages** |
| `Code.gs` | 데이터 API | **Google Apps Script** |

- **화면(index.html)이 바뀌면** → GitHub에 push
- **데이터 로직(Code.gs)이 바뀌면** → Apps Script에 붙여넣고 새 버전 배포
- **둘 다 바뀌면** → 둘 다 해야 함

대시보드 주소: https://kim2t57-hash.github.io/runport-dashboard/

---

## A. index.html 수정 후 배포 (GitHub)

로컬 파일이 수정되어 있으므로 터미널에서 아래 명령어를 실행합니다.

```bash
cd /Users/euntaekkim/Desktop/Claude-VS/.claude/dashboard
git add index.html
git commit -m "변경 내용 한 줄 설명"
git push origin main
```

push 후 **1~2분** 기다리면 대시보드에 자동 반영됩니다.

---

## B. Code.gs 수정 후 배포 (Apps Script)

### 1. Apps Script 편집기 열기

1. [script.google.com](https://script.google.com) 접속 → 해당 프로젝트 클릭
   또는 구글 시트 → **확장 프로그램 → Apps Script**

### 2. Code.gs 붙여넣기

1. 왼쪽에서 `Code.gs` 클릭
2. 전체 선택(Cmd+A) 후 삭제
3. 수정된 `Code.gs` 내용 전체 복사 붙여넣기
4. **Ctrl+S** 저장 (탭에 저장 안 된 표시 ● 없어야 함)

### 3. 새 버전으로 배포

1. 오른쪽 위 **"배포"** 클릭
2. **"기존 배포 관리"** 클릭
3. 연필(✏️) 아이콘 클릭
4. 버전 드롭다운 → **"새 버전"** 선택
5. **"배포"** 클릭

> 저장 없이 배포하면 이전 코드로 버전이 만들어지니 반드시 Ctrl+S 먼저!

---

## 문제가 생기면?

| 증상 | 원인 | 해결 |
|------|------|------|
| 화면 변경이 안 보임 | index.html을 GitHub에 push 안 함 | `git push` 실행 후 1~2분 대기 |
| 데이터가 안 나옴 | Code.gs 배포 안 됨 | Apps Script에서 새 버전 배포 |
| Apps Script 오류 | 코드 문법 오류 | 편집기에서 `getData` 함수 직접 실행(▶)해서 오류 확인 |
| 권한 오류 | 액세스 허용 필요 | 배포 시 "액세스 허용" 팝업 클릭 |

---

## 데이터만 바뀔 때

구글 시트에 데이터가 추가되는 것은 배포 없이 자동 반영됩니다.
대시보드가 1분마다 자동 새로고침하기 때문입니다.
